# Furever 用户反馈周报交接说明

## 1. 任务目标

每周一整理上一自然周（周一 00:00 至周日 23:59，`Asia/Shanghai`）的用户反馈，创建一份新的飞书周报并交给负责人审核。负责人确认后再自行发送给老板。

执行者不能覆盖历史周报、不能删除文档评论，也不负责直接把周报发送给老板。

## 2. 需要安装的 Skill

Skill 名称：`furever-weekly-feedback-report`

安装完成后，在 Codex 中通过 `$furever-weekly-feedback-report` 明确调用。Skill 已包含：

- 数据来源、时间和去重口径；
- 报告八部分结构及 Section 2、Section 3 的固定格式；
- Gmail、Typeform、App Store、Discord 的普通反馈处理规则；
- 后台图片和视频 AI 质量分析规则；
- 频次、原文、优先级和严重度判断规则；
- 飞书文档创建、图片内嵌和最终校验要求；
- 自动采集、分析清单、校验和回归测试脚本。

## 3. 必须开通的权限

### 飞书

使用者自己的飞书账号或自动化身份需要：

1. `Feedback Collection` 多维表格的只读权限，包括 Gmail、Typeform、App Store、Discord 四张表；
2. 批准版参考周报和需要用于环比的历史周报只读权限；
3. 周报目标文件夹的创建和编辑权限；
4. 文档内图片及相关媒体链接的访问权限；
5. 使用者本人账号的 `lark-cli` 授权。

只有在需要读取或回复飞书评论时才开通评论权限。流程不需要删除评论的权限。

### Furever 后台

使用者需要只读访问：

`https://admin.fureverworld.com/dashboard/feedback`

权限至少覆盖：

1. 按日期、媒体类型、Snap Type 和后台原始 Tag 筛选反馈；
2. 打开 Snap 详情页；
3. 查看或导出 Feedback ID、创建时间、User ID、Snap/Generation ID、Snap Type、后台原始 Tag 和用户原文；
4. 查看用户输入图、AI 生成图和 AI 生成视频；
5. 在详情页直接核验是否有 Character Sheet；
6. 打开后台记录引用的存储和媒体链接。

不需要后台写入、删除、退款、订阅修改或用户管理权限。不要共享负责人的飞书会话、后台密码、Cookie、访问令牌或 API 密钥。

## 4. 首次使用前的权限烟测

新执行者第一次使用时，应先完成以下只读检查：

1. 能读取批准版参考周报的目录和结构；
2. 能分页读取 `Feedback Collection` 中一天的 Gmail、Typeform 和 App Store 记录；
3. 能打开一条图片反馈和一条视频反馈；
4. 能看到输入图、输出图、视频链接、后台原始 Tag 和 Character Sheet 状态；
5. 经负责人明确允许后，在目标文件夹新建一份一次性测试文档，并成功内嵌一张图片。

任一项失败都必须明确指出缺少的资源或权限，不能静默跳过某个数据源。

## 5. 每周标准执行方式

### 第一阶段：只做数据检查和方案，不写飞书

在一个新的 Codex 任务中发送：

```text
使用 $furever-weekly-feedback-report，生成上一自然周的 Furever 用户反馈周报。先完成权限检查、数据采集、输入校验和写作方案；只给我方案，不要创建或修改任何飞书文档。不要依赖其他任务中的对话信息。
```

这一阶段应确认：

- 报告周和对照周的起止时间；
- Gmail、Typeform、App Store、Discord 的原始数、去重数和有效数；
- 普通反馈与后台 AI 质量反馈使用不同分母；
- Gmail 只统计周期内用户本人发出的正文，不把客服回复或旧消息当作本周反馈；
- 后台图片、视频事件数及原始 Tag 频次；
- 数据缺口、不可比指标和小样本限制；
- 八部分写作方案以及当周临时专项问题。

只有负责人明确回复 `ok`、`可以` 或同等含义的批准后，才能进入第二阶段。

### 第二阶段：新建并完成周报

负责人批准后发送：

```text
方案通过。请按 $furever-weekly-feedback-report 新建本周飞书周报，不得覆盖或复用历史周报。完成全部分析、图片内嵌和最终校验后，返回新文档链接、校验结果和仍未解决的数据限制。
```

## 6. 人工审核重点

交付后按下面顺序检查：

1. 开头核心结论是否一眼说明本周最应关注的一个问题，并包含 `n/N`、用户影响和立即动作；
2. 高亮块是否只出现在开头核心结论和 Section 2；Section 1、Section 3–8 不应出现高亮块；
3. Section 3 是否严格使用批准的七列表格，`Team Comment` 是否留空；
4. P1、P2 与 S1、S2、S3 是否按证据独立判断；高频本身不能直接等于 S1；
5. Section 4 是否只放未进入 P1/P2 的补充信号，且没有重复 Section 5；
6. Section 5 是否没有高亮框，并为每个 P1/P2 问题放置 3 条标注清楚的用户原文；不足 3 条时是否展示全部并说明不足；
7. AI 图片和视频是否分开统计后台原始 Tag；
8. AI 质量明细是否具备 10 个固定字段，输入图和生成图是否直接显示，视频链接是否可点击；
9. Character Sheet 是否来自逐条打开 Snap 详情核验，而不是根据 Prompt 或 Snap Type 推断；
10. Gmail 小节标题是否为 `8.1 Gmail｜主要反馈主题与代表原文`；
11. 比例是否全部展示分子、分母和百分比；
12. 用户原文是否可追溯且已移除邮箱等不必要的直接身份信息；
13. 是否保留了全部审核评论，只修改评论指出的问题；
14. 是否创建了新文档，没有修改历史周报。

## 7. 如何确认 Skill 真的可交接

不要在创建 Skill 的旧任务里测试。让新执行者在自己的电脑、自己的账号、一个全新的 Codex 任务中，依次执行：

1. `references/acceptance-test.md` 的 Stage 1：固定历史周期，只做权限检查、数据范围和方案，不能写文档；
2. 人工确认 Stage 1 通过后，执行 Stage 2：新建标题以 `【Skill 回归测试】` 开头的测试文档；
3. 对照验收清单逐项检查结构、数据、原文、媒体和最终校验；
4. 如果失败，先按 Access、Workflow、Data、Presentation、Analysis 五类定位问题，再决定是补权限、修脚本还是修改规则。

自动回归测试至少需要通过：

```text
python3 scripts/self_test.py
```

当前交接版本共有 18 项依赖无关测试。正式周报在交付前还必须运行最终报告校验，并得到 `"ok": true`。

## 8. 临时需求与 Skill 修改边界

每周临时关注的问题，例如低星原因、Gift 开放回答、Companion Mode 或 Living Space，应写在当周提示词和方案中，不默认固化为永久章节。

只有当同一种问题会在不同周重复出现，并且属于稳定的数据、结构、展示或校验规则时，才修改 Skill。修改后必须重新运行自动测试和完整验收测试。

## 9. 最终交付标准

合格的周报交付只包含：

- 新建的飞书文档链接；
- 本次使用的数据范围；
- 自动校验结果；
- 仍未解决的数据缺口或展示失败。

负责人审核通过后，再由负责人决定是否发送给老板。
